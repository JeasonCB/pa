/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  images: {
    domains: ['nextui.org', 'github.com', 'media.geeksforgeeks.org']
  }
}

module.exports = nextConfig
